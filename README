# GIMoE: Cluster-Aware Time-Series Forecasting

本仓库是 `project` 的实验代码，主要研究面向多变量时间序列预测的
**簇感知预测架构** 和 **GIMoE / penalty-guided residual experts**。

当前主线不是大模型式通用预训练，而是一个可解释、可控的轻量预测框架：

1. 先根据通道相关性或周期画像对变量分簇。
2. 每个簇拥有独立预测专家，避免所有通道共享同一预测头。
3. GIMoE 根据输入、簇特征和 penalty 画像选择合适的 residual expert。
4. penalty-guided residual experts 以残差形式修正 base prediction。
5. 迁移实验通过簇画像和通道匹配展示同源数据集上的 zero-shot / fine-tune 潜力。

> 当前仓库也包含 adaptive participation 的探索代码，用于研究
> “什么时候启用 penalty、启用哪个 penalty”。这部分目前作为未来方向和诊断工具保留，
> 不建议直接作为当前论文主贡献包装。

## Repository Scope

核心贡献建议聚焦：

- **Cluster-aware forecasting**：通道聚类后按簇训练预测头。
- **GIMoE residual experts**：用 penalty-guided residual experts 做可控修正。
- **Interpretability**：输出 cluster portrait、penalty routing、lambda 和 gate 诊断。
- **Transferability**：同源 ETT 数据集之间展示 zero-shot transfer 能力，作为结构带来的附加能力。

需要谨慎表述：

- KNN hybrid 是可选后处理，不作为 GIMoE 主贡献。
- calibration 相关实验已降级为对照或诊断，不作为主方法。
- adaptive participation 仍处在探索阶段，当前更适合作 future work。

## Project Layout

```text
.
├── configs/                         # 训练、迁移和 fine-tune 配置
│   ├── ETTh1.yaml
│   ├── ETTh2.yaml
│   ├── ETTm1.yaml
│   ├── ETTm2.yaml
│   ├── ETTm1ToETTh1.yaml
│   ├── ETTm1ToETTh2.yaml
│   ├── ETTm1ToETTm2.yaml
│   ├── ETTm2ToETTm1.yaml
│   ├── weather.yaml
│   ├── electricity.yaml
│   └── traffic.yaml
├── data/                            # 数据集 CSV / NPZ / 原始文件
├── outputs/                         # 实验输出，默认不作为代码逻辑依赖
├── paper_figures/                   # 论文图、架构图和 transfer 图
├── scripts/                         # 搜索、消融、迁移、可视化脚本
└── src/
    ├── train.py                     # 主训练入口
    ├── transfer.py                  # zero-shot transfer 入口
    ├── data/                        # CSV 读取与滑窗
    ├── models/                      # predictor / gate / lambda / residual MoE
    └── utils/                       # clustering / metric / plotting / KNN / memory
```

## Environment

推荐使用已有 Conda 环境 `my_fram`：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -c "import torch; print(torch.cuda.is_available())"
```

基础依赖：

```powershell
pip install numpy pandas torch scikit-learn pyyaml matplotlib psutil
```

如果没有 GPU，把配置里的设备改为：

```yaml
exp:
  device: cpu
```

## Data Format

输入数据是带表头的 CSV。默认第 0 列为时间列，其余列为浮点通道值。

```csv
date,HUFL,HULL,MUFL,MULL,LUFL,LULL,OT
2016-07-01 00:00:00,5.827,2.009,1.599,0.462,4.203,1.341,30.531
```

典型配置：

```yaml
data:
  csv_path: data/ETTm1.csv
  date_col: 0
  max_rows: 57600
  train_ratio: 0.6
  val_ratio: 0.2
  test_ratio: 0.2
window:
  input_len: 336
  pred_len: 96
  past_context: true
normalize:
  global_zscore: true
  train_only: true
cluster:
  train_only: true
```

## No-Leak Protocol

当前实验默认遵循以下口径：

- normalization 统计量只从 train split 估计：`normalize.train_only=true`。
- channel clustering 只使用 train split：`cluster.train_only=true`。
- 参数选择、gate calibration、transfer route selection 尽量基于 validation。
- test split 只用于最终报告，不用于调参。
- adaptive oracle diagnostics 默认只在 train/val 上使用，`report_test_oracle=false`。
- `run_summary.json -> leakage_audit` 会记录关键泄露相关开关。

如果是搜索脚本，建议先设置：

```yaml
eval:
  skip_test: true
```

待 validation 选择完成后，再对最终候选做一次 test。

## Quick Start

单数据集训练：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.train --config configs\ETTm1.yaml
```

其他 ETT 配置：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.train --config configs\ETTh1.yaml
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.train --config configs\ETTh2.yaml
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.train --config configs\ETTm2.yaml
```

常用大数据集：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.train --config configs\weather.yaml
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.train --config configs\electricity.yaml
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.train --config configs\traffic.yaml
```

训练输出位于配置中的 `exp.out_dir`，核心文件包括：

```text
run_summary.json
test_metrics.csv
cluster_penalty_probs.csv
cluster_lambda_stats.csv
best_checkpoint.pt
cluster_memory.pt
```

## Core Architecture

### Cluster-Aware Predictor

`src.models.cluster_predictor` 根据 `cluster_id_c` 调用对应簇的预测头。

常用预测头：

- `mlp`
- `dlinear`
- `nlinear`
- `patchtst`
- `gru`
- `tcn`

当前 ETT 主配置主要使用 `mlp`，并固定：

```yaml
window:
  input_len: 336
model:
  predictor: mlp
```

### GIMoE / Penalty-Guided Residual Experts

GIMoE 由三部分组成：

- **MoE gate**：根据簇特征、输入历史和 penalty context 选择 penalty。
- **penalty-guided residual experts**：每个 penalty 对应独立 residual expert。
- **validation-selected residual scaling**：用 validation 控制 residual 是否参与以及参与强度。

典型配置片段：

```yaml
moe:
  enable: true
  topk: 1
  select_ranks: [1]
  gate_route_on_penalty_only: true
  pred_side_residual:
    enable: true
    feature_mode: legacy
    alpha_scale: 1.1
    selection_policy: val_mse_gate
    detach_routed_penalty_pred: false
penalties:
  enabled:
    - level
```

解释：

- `penalties.enabled` 决定 residual experts 的候选集合。
- `topk` / `select_ranks` 决定 gate 选择哪些 penalty。
- `pred_side_residual.selection_policy` 决定如何基于 validation 选择 residual 输出。
- `detach_routed_penalty_pred=false` 保留 residual 路径对训练的影响。

### Adaptive Participation

当前代码支持：

```yaml
moe:
  pred_side_residual:
    adaptive_participation:
      enable: true
      label_scope: positive_or_routed
      target_rate_weight: 0.1
      report_threshold: 0.35
```

这部分用于探索：

- 这种 input 是否应该启用 penalty。
- 哪个 penalty 更可能有效。
- continuous participation probability 与 oracle improvement 的关系。

目前实验显示 penalty 选择信号较清晰，但启停更像连续弱参与，不应作为当前主结论强行二值化。

## Current Reference Results

### ETT Horizon Sweep

参考文件：

```text
outputs/ett_horizon_sweep/results.csv
```

该表统一跑了四个 ETT 数据集的 `96 / 192 / 336 / 720` 步预测，`input_len=336`。
下表列出 base test MSE：

| Dataset | H=96 | H=192 | H=336 | H=720 |
|---|---:|---:|---:|---:|
| ETTh1 | 0.3617 | 0.4120 | 0.4323 | 0.4525 |
| ETTh2 | 0.2872 | 0.3630 | 0.3904 | 0.4199 |
| ETTm1 | 0.2845 | 0.3282 | 0.3694 | 0.4335 |
| ETTm2 | 0.1651 | 0.2345 | 0.2875 | 0.4192 |

说明：

- `base_test_mse` 是非 KNN hybrid 的模型测试结果。
- `selected_test_mse` 可能根据 validation 策略选择 base 或 hybrid。
- 若论文主张 GIMoE，需要优先使用 matched on/off ablation，而不是 hybrid selected 数字。

### ETTm1 H96 GIMoE On/Off

参考文件：

```text
outputs/ettm1_h96_gimoe_2pct_refine_summary.csv
```

matched on/off 口径下，当前可复现的正收益配置：

| Candidate | MoE Off MSE | MoE On MSE | Gain |
|---|---:|---:|---:|
| level_lam020 | 0.292128 | 0.285406 | 2.30% |
| level_lam250 | 0.292128 | 0.285446 | 2.29% |
| level_lam005 | 0.292128 | 0.286012 | 2.09% |
| level_valscale | 0.292128 | 0.286281 | 2.00% |

当前结论：

- GIMoE 的 no-leak 正收益是存在的。
- 收益更稳定地来自 `level` 类 bias/level correction，而不是所有 penalty 同时参与。
- 强行要求每个模块单独提升并不合理，论文中应强调整体 GIMoE 配置的增益和可解释性。

## Transfer Experiments

迁移入口：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.transfer --config configs\ETTm1ToETTh1.yaml
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.transfer --config configs\ETTm1ToETTh2.yaml
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m src.transfer --config configs\ETTm1ToETTm2.yaml
```

transfer 配置核心字段：

```yaml
source:
  memory_path: outputs/ETTm1/cluster_memory.pt
  checkpoint_path: outputs/ETTm1/best_checkpoint.pt
transfer:
  route_fit_scope: train
  corr_mode: cycle_template
  corr_align: head
  resample:
    enable: true
```

当前推荐表述：

- zero-shot transfer 是结构带来的自然能力展示。
- 在 ETTh1 / ETTh2 等数据较少的同源目标上更有优势。
- 在 ETTm2 等数据充分目标上接近或略弱于 self-training 是合理现象。
- cross-domain 到 Traffic / Electricity / Weather 需要作为 limitation 谨慎报告。

## Fine-Tune Transfer

fine-tune 脚本：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\run_ettm1_finetune_transfer.py
```

建议 protocol：

- source checkpoint 作为初始化。
- target train split 训练。
- target validation early stopping。
- learning rate 使用 from-scratch 的 0.1 到 0.2 倍。
- 不用 test 调参。

论文中建议分开报告：

- self-training
- zero-shot transfer
- fine-tune transfer

## Ablation and Search Scripts

### MoE On/Off

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\compare_moe_on_off.py --config configs\ETTm1.yaml
```

### ETTm1 H96 GIMoE Search

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\run_ettm1_h96_gimoe_gain_search.py
```

### Module / Backbone Ablation

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\run_ettm1_ablation.py
```

### Full ETT Horizon Sweep

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\run_ett_horizon_sweep.py
```

输出：

```text
outputs/ett_horizon_sweep/results.csv
```

### Penalty / Lambda Validation Search

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\run_ett_penalty_lambda_val_search.py
```

## Visualization

主架构图：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\draw_gimoe_architecture.py
```

常用诊断图：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\plot_ett_val_calibration_clean.py
F:\Anaconda3\Scripts\conda.exe run -n my_fram python scripts\summarize_ettm1_full_target_transfer.py
```

输出通常位于：

```text
paper_figures/
outputs/*/
```

## Output Summary

单次训练的 `run_summary.json` 中常用字段：

- `val.avg_mse`
- `test.avg_mse`
- `test_hybrid.avg_mse`
- `selected.variant`
- `selected.avg_mse`
- `moe_residual_selection`
- `moe_gate_penalty_hit`
- `adaptive_participation`
- `leakage_audit`
- `timing`
- `resources`

每通道测试指标：

```text
test_metrics.csv
```

路由和 penalty 统计：

```text
cluster_penalty_probs.csv
cluster_lambda_stats.csv
```

## Paper-Oriented Narrative

推荐论文叙事：

1. 多变量时间序列中，不同通道并不共享完全一致的动态模式。
2. 先做通道聚类，让预测头具备簇感知能力，可以稳定提升基础预测能力。
3. 单一全局 penalty 对所有通道并不合适，因此引入 GIMoE，让不同簇和输入选择不同 residual expert。
4. penalty-guided residual experts 以残差方式修正 base prediction，保持主预测器稳定。
5. 路由概率、penalty 画像和 residual selection 提供可解释性。
6. 同源 transfer 展示该结构具备一定 zero-shot 可迁移性，但 cross-domain transfer 仍是限制。

建议主动承认：

- GIMoE 的收益依赖 penalty 组合和 lambda，不能声称所有 penalty 都单独有效。
- adaptive participation 仍是未来工作。
- transfer 只作为辅助能力展示，不作为核心 SOTA 结论。

## Useful Checks

编译检查：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -m py_compile src\train.py src\transfer.py src\models\residual_moe.py
```

YAML 检查：

```powershell
F:\Anaconda3\Scripts\conda.exe run -n my_fram python -c "import yaml; [yaml.safe_load(open(p, encoding='utf-8')) for p in ['configs/ETTh1.yaml','configs/ETTh2.yaml','configs/ETTm1.yaml','configs/ETTm2.yaml']]; print('ok')"
```

只跑 validation、不碰 test：

```yaml
eval:
  skip_test: true
```

## Notes

- 本 README 面向 `project` 仓库，不是 `project2`。
- 当前本地实验目录可能包含大量历史搜索产物；论文引用结果时应明确对应 CSV 和 run directory。
- test-driven 搜索结果必须标注为 test-optimized；validation-selected 结果才能作为更干净的泛化报告。
