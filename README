# MoELoss

MoELoss 是一个面向多变量时间序列预测的实验项目。项目先按通道相关性进行聚类，再为每个聚类训练独立预测专家，并用 MoE 路由器选择不同预测惩罚项参与训练。当前代码还支持动态 lambda、聚类画像、KNN shape hybrid 后处理、跨数据集迁移和多数据集多预测长度批量实验。

## 目录结构

```text
.
├── configs/                         # 训练、迁移和微调配置
│   ├── ETTh1.yaml
│   ├── ETTm1.yaml
│   ├── ETTm1ToETTh1.yaml            # src.transfer 使用的迁移配置
│   └── finetune/ETTh1_from_ETTm1.yaml
├── data/                            # CSV 数据集
├── outputs/                         # 训练结果、模型、图像和批量实验结果
├── scripts/                         # 实验、分析、批量运行和 CSV->Excel 脚本
└── src/
    ├── train.py                     # 主训练入口
    ├── transfer.py                  # 跨数据集迁移评估入口
    ├── data/                        # CSV 读取和滑窗构造
    ├── models/                      # 聚类预测器、MoE gate、lambda、penalty
    └── utils/                       # 聚类、指标、画像、KNN hybrid、绘图等工具
```

## 环境准备

建议使用 Python 3.10 或 3.12。GPU 训练需要安装与本机 CUDA 匹配的 PyTorch。

```powershell
pip install numpy pandas torch scikit-learn pyyaml matplotlib psutil
```

说明：

- `psutil` 只用于统计资源占用，缺失时训练仍可运行。
- `scripts/export_result_to_excel.py` 使用 Python 标准库生成 `.xlsx`，不依赖 `openpyxl` 或 Excel 本体。
- 如果只在 CPU 上运行，把配置文件里的 `exp.device` 改为 `cpu`。

## 数据格式

输入数据是带表头的 CSV。默认第 0 列是时间列，其余列必须是可转为浮点数的通道值。

```csv
date,HUFL,HULL,MUFL,MULL,LUFL,LULL,OT
2016-07-01 00:00:00,5.827,2.009,1.599,0.462,4.203,1.341,30.531
```

相关配置：

```yaml
data:
  csv_path: data/ETTh1.csv
  date_col: 0
  train_ratio: 0.7
  val_ratio: 0.1
  test_ratio: 0.2
window:
  input_len: 336
  pred_len: 720
```

`input_len` 是历史窗口长度，`pred_len` 是预测长度。代码会在 train/val/test 各自切分内构造严格滑窗，输出形状为 `[N, C, L]` 和 `[N, C, H]`。

## 快速开始

### 单数据集训练

```powershell
python -m src.train --config configs/ETTh1.yaml
```

常用配置也可以直接替换：

```powershell
python -m src.train --config configs/ETTm1.yaml
python -m src.train --config configs/ETTm2.yaml
python -m src.train --config configs/weather.yaml
python -m src.train --config configs/electricity.yaml
```

训练完成后，主要结果会写入配置中的 `exp.out_dir`，例如 `outputs/ETTh1`。

### ETTm1 到 ETTh1 微调

先训练源域 ETTm1，再运行微调配置：

```powershell
python -m src.train --config configs/ETTm1.yaml
python -m src.train --config configs/finetune/ETTh1_from_ETTm1.yaml
```

微调配置中的关键字段：

```yaml
finetune:
  enable: true
  checkpoint_path: outputs/ETTm1/best_checkpoint.pt
  memory_path: outputs/ETTm1/cluster_memory.pt
  cluster_map: corr
  strict_window: true
  strict_model: true
```

`cluster_map: corr` 会根据源域和目标域聚类原型的相关性建立目标聚类到源聚类的映射。`strict_window` 和 `strict_model` 为 `true` 时，要求源模型窗口长度、预测长度和模型结构与目标配置匹配。

### 跨数据集迁移评估

`src.transfer` 使用源模型的 checkpoint 和 cluster memory，在目标数据集上做聚类映射和测试评估。

```powershell
python -m src.transfer --config configs/ETTm1ToETTh1.yaml
python -m src.transfer --config configs/ETTm1ToETTm2.yaml
python -m src.transfer --config configs/ETTm1ToTraffic.yaml
```

迁移配置中的 `source` 指向源域产物，`transfer.corr_mode` 控制目标通道如何匹配源聚类：

```yaml
source:
  memory_path: outputs/ETTm1/cluster_memory.pt
  checkpoint_path: outputs/ETTm1/best_checkpoint.pt
transfer:
  corr_mode: cycle_template
  corr_align: head
  resample:
    enable: true
    target_step_minutes: 15
```

## 批量实验

`scripts/run_all_datasets_horizons.py` 会为多个配置和多个预测长度生成临时配置、串行训练，并汇总长表和宽表结果。

运行所有默认数据集和默认预测长度：

```powershell
python scripts/run_all_datasets_horizons.py --device cuda:0
```

默认批量规则：

- 普通数据集使用预测长度 `96 192 336 720`，并保留各自配置文件里的 `window.input_len`。
- `bandwidth` / `bandwidth_multi` 使用预测长度 `24 48 96 168`，且每个生成配置都会固定设置 `window.input_len = 24`。
- 如果显式传入 `--horizons`，脚本会使用命令行给定的预测长度；bandwidth 仍会固定使用 24 步输入窗口。

只运行指定数据集和预测长度：

```powershell
python scripts/run_all_datasets_horizons.py `
  --configs configs\electricity.yaml `
  --horizons 336 720 `
  --device cuda:0 `
  --knn-selection-policy hybrid
```

常用参数：

- `--configs`：显式指定一个或多个 YAML，优先级高于自动发现。
- `--horizons`：覆盖预测长度；普通数据集默认是 `96 192 336 720`，bandwidth 默认是 `24 48 96 168`。
- `--out-root`：批量输出根目录，默认 `outputs/all_datasets_horizons`。
- `--epochs`：覆盖所有生成配置的训练轮数，适合快速验证。
- `--reuse-existing`：如果目标目录已有 `run_summary.json`，直接读取汇总，不重新训练。
- `--dry-run`：只生成计划和配置，不启动训练。
- `--keep-artifacts`：保留图、画像、checkpoint、memory 等产物。默认批量脚本会关闭部分重量级产物以节省空间。
- `--disable-knn-hybrid`：禁用 KNN hybrid。
- `--knn-selection-policy`：控制最终选择 base 还是 hybrid，可选 `hybrid`、`val_mse_margin`、`val_mse`、`base`。

批量结果默认写入：

```text
outputs/all_datasets_horizons/results.csv
outputs/all_datasets_horizons/results_wide.csv
outputs/all_datasets_horizons/configs/*.yaml
outputs/all_datasets_horizons/<dataset>/pred_<horizon>/
```

## 导出 Excel

把 `outputs/result.csv` 导出为 Excel：

```powershell
python scripts\export_result_to_excel.py
```

默认输出：

```text
outputs/result.xlsx
```

指定输入和输出：

```powershell
python scripts\export_result_to_excel.py outputs\all_datasets_horizons\results.csv -o outputs\all_datasets_horizons\results.xlsx
```

可选参数：

- `--sheet-name`：指定工作表名称。
- `--encoding`：指定 CSV 编码；默认依次尝试 `utf-8-sig`、`utf-8`、`gb18030`。
- `--no-infer-numbers`：所有单元格按文本写入，不自动把数字字符串转成数值。

## 核心训练流程

`src.train` 的主要流程如下：

1. 读取 CSV，并按 `data.train_ratio`、`val_ratio`、`test_ratio` 切分。
2. 根据 `normalize.global_zscore` 做全局 z-score 标准化；如果启用 `normalize.train_only`，均值和标准差只从训练段估计。
3. 计算通道 Pearson 相关矩阵，或读取已有 `corr.save_path`。
4. 根据 `cluster.method` 对通道聚类，得到每个通道的 `cluster_id`。
5. 为每个聚类构建预测专家，即 `model.predictor` 指定的模型。
6. 计算基础 MSE 和启用的 penalty，并由 MoE gate 选择 penalty 路由。
7. 使用固定 lambda、动态 lambda 或 learnable lambda 对 penalty 加权。
8. 根据 `train.selection_metric` 和 early stopping 保存每个聚类的最佳状态。
9. 可选构建 KNN shape bank，对 base prediction 做 hybrid 后处理。
10. 在 test split 输出 MAE/MSE、每通道指标、cluster penalty 概率和运行摘要。

## 配置说明

### exp

```yaml
exp:
  name: cluster_moe_example
  out_dir: outputs/ETTh1
  seed: 2026
  deterministic: true
  device: cuda:0
```

- `out_dir`：所有训练产物的输出目录。
- `deterministic`：启用更稳定的随机性控制。
- `device`：优先使用的设备。如果 CUDA 不可用，代码会回退到 CPU。

### corr 和 cluster

```yaml
corr:
  compute: true
  save_path: outputs/ETTh1/corr.npy
cluster:
  method: leader
  n_clusters: 3
  distance_threshold: 0.7
  merge_small_clusters: true
```

支持的聚类方法包括：

- `leader` / `greedy_leader`
- `agglomerative` / `agglo`
- `kmeans` / `k-means`
- `spectral` / `spectral_clustering`
- `dbscan`
- `random` / `rand`

`distance_threshold` 常用于 `leader` 和层次聚类；`merge_small_clusters` 会把过小聚类合并到更相近的大聚类。

### model

```yaml
model:
  predictor: mlp
  hidden_dim: 128
  dropout: 0.35
```

支持的 `model.predictor`：

- `mlp` / `cluster_mlp`
- `attn_mlp`
- `context_mlp`
- `nlinear`
- `dlinear`
- `patchtst` / `patch_transformer`
- `nbeats`
- `gru`
- `tcn`

额外开关：

- `revin: true`：为预测器包一层 RevIN。
- `seasonal_residual: true`：用周期残差包装预测器。

### penalties

```yaml
penalties:
  enabled:
    - jump
    - smooth
    - level
    - delta
  jump_threshold: 0.6
```

支持的 penalty：

- `amp`：预测和真实值的波动幅度对齐。
- `jitter`：二阶差分平滑约束。
- `jump`：在真实序列存在明显跳变的位置约束预测跳变。
- `smooth`：一阶差分平滑约束。
- `level`：均值/水平对齐。
- `delta`：一阶差分对齐。

### moe

```yaml
moe:
  enable: true
  topk: 1
  gate_hidden_dim: 64
  select_ranks: [1]
  gate_entropy_weight: 0.02
  gate_balance_weight: 0.01
  router_mode: penalty_context
  allow_skip: true
  skip_cost: 0.15
  lambda_init:
    jump: 0.1
    smooth: 0.1
    level: 0.1
    delta: 0.1
```

关键字段：

- `enable`：是否启用 MoE penalty 路由。
- `topk`：gate 每个聚类选择的 penalty 数量。
- `select_ranks`：可以只选指定排序位置的 penalty，例如 `[1]` 表示只取概率最高项。
- `router_mode: penalty_context`：在路由时融合当前 penalty 上下文。
- `allow_skip`：允许 gate 跳过 penalty。
- `lambda_init`：每个 penalty 的基础权重。
- `lambda_min` 和 `lambda_schedule`：控制 lambda 下限和调度。

动态 lambda：

```yaml
moe:
  dynamic_lambda:
    enable: true
    mode: multiscale
    hidden_dim: 24
    segment_bins: [4, 8]
    max_factor: 1.5
    mix: 0.6
    reg_weight: 0.0001
```

Learnable lambda：

```yaml
moe:
  learnable_lambda:
    enable: false
    reg_weight: 0.01
    bilevel:
      enable: true
      outer_lr: 0.005
      inner_lr: 0.001
```

### train 和 early_stop

```yaml
train:
  epochs: 100
  batch_size: 64
  lr: 0.001
  mse_weight: 0.9
  selection_metric: val_mse
  weight_decay: 0.0002
  grad_clip: 1.0
  penalty_warmup_epochs: 15
early_stop:
  patience: 10
  min_delta: 1.0e-06
```

`selection_metric` 可选：

- `val_loss`
- `val_mse`
- `train_loss`
- `train_mse`

当前常用配置选择 `val_mse`，即按验证集 MSE 选择最佳聚类专家状态。

### knn_hybrid

```yaml
knn_hybrid:
  enable: true
  mode: rolling
  scope: same_cluster
  bank_split: history
  k: 256
  alpha: 1.4
  feature_mode: joint
  template_mode: residual
  adaptive_alpha: agreement
  selection_policy: val_mse_margin
```

KNN hybrid 用历史窗口的形状特征检索相似样本，再把检索模板与模型预测融合。常用字段：

- `mode`：`fixed` 或 `rolling`。
- `scope`：`same_cluster` 或 `same_channel`。
- `bank_split`：使用哪个切分构建检索库。
- `k`：近邻数量。
- `alpha`：KNN 修正强度。
- `alpha_horizon_ref`、`alpha_horizon_power`：按预测长度缩放 alpha。
- `feature_mode`：例如 `hist` 或 `joint`。
- `template_mode`：例如 `future` 或 `residual`。
- `adaptive_alpha`：`none`、`agreement`、`distance`、`confidence`、`distance_agreement`。
- `selection_policy`：最终结果选择策略。

## 输出文件

单次训练输出目录通常包含：

```text
outputs/<dataset>/
├── run_summary.json              # 总结：配置、best_epoch、val/test、selected、timing、resources
├── test_metrics.csv              # 每通道 MAE/MSE
├── best_checkpoint.pt            # 最佳模型、gate、lambda 和必要 meta
├── cluster_memory.pt             # 聚类原型和通道聚类信息
├── cluster_penalty_probs.csv     # 每个聚类的 penalty 选择概率和 skip 统计
├── cluster_lambda_stats.csv      # 动态 lambda 统计
├── corr.npy                      # 通道相关矩阵
├── knn_shape_bank.pt             # KNN hybrid 检索库
├── loss_curves/                  # 每聚类训练/验证曲线
├── plots/                        # 随机、best、worst 预测图
└── cluster_portraits/            # 聚类画像和雷达图
```

`run_summary.json` 中最重要的字段：

- `test.avg_mae`、`test.avg_mse`：base 模型测试结果。
- `test_hybrid.avg_mae`、`test_hybrid.avg_mse`：启用 KNN hybrid 后的测试结果。
- `selected.variant`：最终选择 `base` 还是 `hybrid`。
- `selected.avg_mae`、`selected.avg_mse`：按选择策略得到的最终结果。
- `timing`：总耗时和平均每 epoch 耗时。
- `resources`：CPU/GPU 和输出目录占用。

## 常用脚本

### 对比 MoE 开关

```powershell
python scripts\compare_moe_on_off.py --config configs\ETTm1.yaml
```

用于比较开启/关闭 MoE penalty 路由时的结果，并保存样本窗口图和汇总 CSV。

### KNN shape 变体比较

```powershell
python scripts\compare_knn_shape_variants.py --config configs\ETTh1.yaml
```

用于比较 KNN hybrid 的 scope、mode、alpha、k 等设置。

### 可视化通道相关性和聚类

```powershell
python scripts\plot_ettm1_channels_corr.py
python scripts\plot_weather_channels_corr.py
```

会读取对应数据和配置，绘制通道曲线、相关性或聚类摘要图。

### 搜索 ETTm1 penalty lambda

```powershell
python scripts\search_ettm1_lambdas.py
```

用于在 ETTm1 上搜索 penalty 权重组合。运行前请先查看脚本参数，避免一次性启动过大的搜索空间。

## 建议实验流程

1. 先用 `configs/ETTh1.yaml` 或 `configs/ETTm1.yaml` 跑通单次训练。
2. 检查 `run_summary.json`、`test_metrics.csv` 和 `cluster_penalty_probs.csv`。
3. 如果 hybrid 表现稳定，使用 `run_all_datasets_horizons.py` 批量跑多个 horizon。
4. 使用 `scripts/export_result_to_excel.py` 把汇总 CSV 导出为 Excel。
5. 需要迁移时，先确保源域存在 `best_checkpoint.pt` 和 `cluster_memory.pt`，再运行 `src.transfer` 或 fine-tune 配置。

## 常见问题

### CUDA 不可用

如果配置是 `cuda:0` 但机器没有可用 GPU，代码会回退 CPU。为了避免误判速度，建议直接改为：

```yaml
exp:
  device: cpu
```

### 批量结果里 `selected_variant` 是 base 还是 hybrid

由 `knn_hybrid.selection_policy` 决定：

- `hybrid`：总是选 hybrid。
- `base`：总是选 base。
- `val_mse`：验证集 hybrid MSE 更低才选 hybrid。
- `val_mse_margin`：验证集提升超过最小绝对/相对阈值才选 hybrid。

### `run_summary.json not found`

通常表示训练进程失败或提前退出。检查对应运行目录里的 `.log`、`.err.log`，以及批量结果 CSV 的 `error` 列。

### 输出目录太大

批量脚本默认会关闭部分重量级产物。单次训练如果空间紧张，可以在配置里关闭：

```yaml
plot:
  enable: false
portrait:
  enable: false
memory:
  save_checkpoint: false
```

### 如何复用已有结果

批量实验使用：

```powershell
python scripts\run_all_datasets_horizons.py --reuse-existing
```

它会读取已有 `run_summary.json` 重新生成汇总表，而不会重新训练。
